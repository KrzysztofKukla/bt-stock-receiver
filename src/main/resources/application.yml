spring:
  application:
    name: bt-stock-receiver
  #log tracing
  zipkin:
    enabled: false
  mongodb:
    port: 0 #random port
    host: localhost

server:
  port: 8086

eureka:
  client:
    enabled: false
